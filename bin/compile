#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

BUILD_DIR=$1
cd $BUILD_DIR

if [ -f $1/prepare.py ]; then
  echo "-----> Configuring"
  ./prepare.py
else
  exit 0
fi

# make
echo "-----> Compiling with Make"
make
